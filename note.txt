MySQL索引优化 SQL优化
queryParam.orderBy 过滤非法参数，防止SQL注入

错误码格式 1位错误等级码+2位错误应用码+3位错误文件码+2位错误类型码+2位错误识别码
(类型码 01数据库错误 02传入参数错误 03应用程序错误)

用户头像直接通过ID获取，不另存头像的URL

sql explain

用户关注列表在本地缓存，粉丝多的用户新发表的内容放在redis中。
配置文件配置各目录的帖子缓存数量、每个帖子的一级回复缓存数量。每个目录分别缓存热帖和新帖，一级回复亦然。
每发表一个新的内容就放到新帖缓存区，用户查看某目录内容时直接从缓存区拿。用户一直刷到缓存区没有时，才从数据库拿。
用户每查看一个帖子时，浏览量加一。然后该浏览量和热帖缓存区最小的浏览量比较，如果大于最小的浏览量，
则该帖替换掉最小浏览量的帖子。
考虑redis中各个内容的过期时间设置

各种数据结构的线程安全问题

只允许在叶目录发帖，每个大的目录下设置一个“广场”目录，包含各种无需分类的内容，然后再添加各种细分目录

有内容的叶目录不能再设子目录，可以在该叶目录的父目录下新增一个目录，然后将该目录转移到新目录下，并将新目录改成原目录的样子
来实现增设子目录的操作

用户查看目录时首先展示的是子目录，列表用户可以查看子目录的子目录，也可以进入目录查看目录中的帖子。这样做的话，就可以
知道用户是确实要看这个目录，而不是为了看子目录的帖子而不得不先查看父目录的帖子，如果不这样的话，统计用户最近访问的
目录数据就不准确，因为用户可能是为了访问子目录而访问父目录。

新建表的时候自增ID起始值设为1！！！